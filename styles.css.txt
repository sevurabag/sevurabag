* { margin:0; padding:0; box-sizing:border-box; }
body { font-family: Arial, sans-serif; color:#333; line-height:1.6; background:#fff; }
.navbar { display:flex; justify-content:space-between; align-items:center; padding:15px 20px; border-bottom:1px solid #eee; }
.navbar .logo { font-size:26px; font-weight:bold; color:#D16A66; text-decoration:none; }
.menu { list-style:none; display:flex; gap:15px; }
.menu a { text-decoration:none; color:#333; font-weight:500; }
.hero { background:#fdf6f2; text-align:center; padding:80px 20px; }
.hero h1 { font-size:36px; margin-bottom:10px; }
.btn-main { display:inline-block; background:#D16A66; color:#fff; padding:12px 25px; border-radius:6px; text-decoration:none; margin-top:15px; transition:0.3s; }
.btn-main:hover { background:#b25555; }
.products-section { padding:50px 20px; text-align:center; }
.products-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(250px,1fr)); gap:20px; margin-top:25px; }
.product-card { border:1px solid #eee; border-radius:8px; padding:15px; transition:0.3s; }
.product-card:hover { box-shadow:0 5px 15px rgba(0,0,0,0.1); }
.product-card img { width:100%; border-radius:5px; }
.product-card h3 { margin:10px 0 5px; }
.product-card p { font-size:14px; color:#666; }
.price { font-weight:bold; color:#D16A66; margin:10px 0; }
.btn { display:inline-block; background:#333; color:#fff; padding:8px 15px; border-radius:5px; text-decoration:none; transition:0.3s; }
.btn:hover { background:#555; }
.about-section { background:#fafafa; padding:50px 20px; text-align:center; }
.contact-section { padding:50px 20px; text-align:center; }
.contact-section form input,
.contact-section form textarea { width:80%; padding:12px; margin:10px 0; border:1px solid #ccc; border-radius:5px; }
.contact-section form button { background:#D16A66; color:#fff; padding:12px 25px; border:none; border-radius:6px; cursor:pointer; }
.contact-section form button:hover { background:#b25555; }
footer { background:#333; color:#fff; text-align:center; padding:15px; margin-top:50px; }
.admin-section { padding:40px 20px; max-width:500px; margin:auto; }
