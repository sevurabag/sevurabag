backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads" # Görseller bu klasöre gidecek
public_folder: "/images/uploads"

collections:
  - name: "products"
    label: "Ürünler"
    folder: "content/products"
    create: true
    slug: "{{title}}"
    fields:
      - {label: "Ürün Adı", name: "title", widget: "string"}
      - {label: "Satış Fiyatı", name: "price", widget: "string"}
      - {label: "Ürün Görseli", name: "image", widget: "image"}
      - {label: "Shopier Linki", name: "shopier_url", widget: "string"}
      - {label: "Ürün Açıklaması", name: "body", widget: "markdown", required: false}
